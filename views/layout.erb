<!DOCTYPE html>
<html>
  <head>
    <title>Frank's Blackjack</title>
    <link rel="stylesheet" href="/vendor/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/application.css">
  </head>
  <body style="padding-top: 50px;">

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <div>
            <form class="display" action='/place_bet' method='get'>
              <input type='submit' value='Play Again' class='btn btn-primary'/>
            </form>

            <form class="display" action='/reset' method='get'>
              <input type='submit' value='Change Player' class='btn btn-danger'/>
            </form>
          </div>
        </div>
      </div>
    </div>

  <div class="container">
    <%if @error %>
      <div class="alert alert-error"><%=@error%></div>
    <% end %>
    <%if @success %>
      <div class="alert alert-success"><%=@success%></div>
    <% end %>
      <%= yield %>
  </div>
  </body>
</html>
